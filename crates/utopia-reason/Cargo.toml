[package]
name = "utopia-reason"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
uuid.workspace = true
# Attribute values are literals (`facts.object_value`), so comparisons need to recognize their shape
serde_json.workspace = true
