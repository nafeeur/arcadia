[package]
name = "utopia-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
anyhow.workspace = true
uuid.workspace = true
chrono.workspace = true
sqlx.workspace = true
figment.workspace = true
pgvector.workspace = true
strum.workspace = true
aes-gcm.workspace = true
base64.workspace = true

[dev-dependencies]
# Jail: an isolation fixture for environment variables. Regressions in this config layer
# need to be verified against real environment variables — rolling your own with set_var
# just makes parallel tests fight each other (this is figment's test feature).
figment = { workspace = true, features = ["test"] }
